FFAM Work Distribution API Service 

- Pre-requisites : 
  - Make sure that the database is up and running. 
  - If not : docker run -d -p 1521:1521 -v ~/workspace/WorkDistribution/docker/oracle/:/docker-entrypoint-initdb.d mydb:latest